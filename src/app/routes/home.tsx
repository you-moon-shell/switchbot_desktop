/**
 * ホーム（仮置き）。
 * Epic C でテレメトリ + デバイス状態を合成したダッシュボードになる予定。
 */
export default function HomeRoute() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-3 bg-neutral-50 text-neutral-900">
      <h1 className="text-2xl font-bold">SwitchBot Desktop</h1>
      <p className="text-sm text-neutral-500">
        ホーム（仮）— ダッシュボードは Epic C で実装予定
      </p>
    </main>
  );
}
